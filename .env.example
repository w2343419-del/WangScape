# WangScape Writer 环境变量配置示例
# 复制此文件为 .env，然后根据实际情况修改

# ==================== 身份认证配置 ====================

# 管理员用户名 (默认: admin)
ADMIN_USERNAME=admin

# 管理员密码 - 明文模式 (可选，与ADMIN_PASSWORD_HASH二选一)
# 注意: 生产环境建议使用ADMIN_PASSWORD_HASH
ADMIN_PASSWORD=change_me_to_strong_password_123456

# 管理员密码 - 哈希模式 (SHA-256哈希，可选，与ADMIN_PASSWORD二选一)
# 生成SHA-256哈希: echo -n "your_password" | sha256sum
# 或使用在线工具生成，然后转小写
# ADMIN_PASSWORD_HASH=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855

# JWT密钥 (必需，生成: openssl rand -hex 32)
JWT_SECRET=your_jwt_secret_key_here_change_me_to_random_hex_string_32_chars

# JWT令牌有效期 (小时，默认: 24)
JWT_TTL_HOURS=24

# ==================== HTTPS/TLS配置 ====================

# HTTP监听端口 (默认: 8080)
HTTP_PORT=8080

# HTTPS监听端口 (默认: 443)
HTTPS_PORT=443

# TLS证书文件路径 (留空表示不启用HTTPS)
# 可使用Let's Encrypt: certbot certonly --standalone -d example.com
# TLS_CERT_FILE=/etc/letsencrypt/live/example.com/fullchain.pem

# TLS私钥文件路径
# TLS_KEY_FILE=/etc/letsencrypt/live/example.com/privkey.pem

# ==================== 邮件配置 ====================

# SMTP密码 (将在程序中使用SMTP_ENCRYPTION_KEY加密)
SMTP_PASSWORD=your_smtp_password_here

# SMTP密码加密密钥 (AES-256-GCM，生成: openssl rand -hex 16)
# 仅当使用加密密码时需要
SMTP_ENCRYPTION_KEY=your_encryption_key_hex_16_chars

# ==================== 网络配置 ====================

# 是否在代理后面 (true/false，默认: false)
# 当应用程序在Nginx/Apache反向代理后时设置为true
# 这样可以正确获取客户端真实IP
BEHIND_PROXY=false

# ==================== 可选的遗留配置 ====================

# 旧版本兼容的管理员令牌 (不推荐，已被JWT替代)
# ADMIN_TOKEN=your_legacy_admin_token_here

# ==================== 自定义配置 ====================

# 网站基础URL (用于生成正确的链接)
BASE_URL=https://example.com

# 网站标题
SITE_TITLE=My Blog

# 主题配置等其他参数
# 可根据需要添加
